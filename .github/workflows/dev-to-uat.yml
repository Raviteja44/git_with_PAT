name: 2. Promote Dev → UAT

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number that was merged into dev"
        required: true
        type: number
      source_branch:
        description: "Select the source branch for verification (must be dev)"
        required: true
        type: choice
        options:
          - dev

permissions:
  contents: write
  pull-requests: write

jobs:
  promote:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Git and tools
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          sudo apt-get update -y
          sudo apt-get install -y jq

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate PR exists, is merged, and targeted dev
        id: validate
        run: |
          PR="${{ github.event.inputs.pr_number }}"

          if [ -z "$PR" ]; then
            echo "Error: PR number cannot be empty." >&2
            exit 1
          fi

          # Fetch PR metadata
          PR_JSON=$(curl -s \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/pulls/${PR}")

          # Check if PR exists
          ERR=$(echo "$PR_JSON" | jq -r '.message // empty')
          if [ -n "$ERR" ]; then
            echo "Error: PR #$PR not found. API message: $ERR" >&2
            exit 1
          fi

          BASE_BRANCH=$(echo "$PR_JSON" | jq -r '.base.ref')
          MERGED=$(echo "$PR_JSON" | jq -r '.merged')

          # MUST be merged
          if [ "$MERGED" != "true" ]; then
            echo "Error: PR #$PR is not merged." >&2
            exit 1
          fi

          # MUST have been merged into dev
          if [ "$BASE_BRANCH" != "dev" ]; then
            echo "Error: PR #$PR must have been merged into dev (found: $BASE_BRANCH)." >&2
            exit 1
          fi

          # Get last commit SHA
          LAST_SHA=$(curl -s \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/pulls/${PR}/commits" \
            | jq -r '.[-1].sha')

          echo "pr_number=$PR" >> $GITHUB_OUTPUT
          echo "pr_last_sha=$LAST_SHA" >> $GITHUB_OUTPUT

      - name: Download PR diff
        run: |
          PR="${{ steps.validate.outputs.pr_number }}"
          curl -s \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            -H "Accept: application/vnd.github.v3.diff" \
            "https://api.github.com/repos/${{ github.repository }}/pulls/${PR}" \
            > pr.diff

          if [ ! -s pr.diff ]; then
            echo "Error: PR #$PR diff is empty." >&2
            exit 1
          fi

      - name: Create temporary promote branch from uat
        id: create_branch
        run: |
          PR="${{ steps.validate.outputs.pr_number }}"
          TS=$(date +%s)
          BRANCH="promote/dev-to-uat-${TS}-pr${PR}"

          git fetch origin uat:refs/remotes/origin/uat || true
          git checkout -b "$BRANCH" origin/uat

          echo "branch=$BRANCH" >> $GITHUB_OUTPUT

      - name: Apply PR diff and push temp branch
        run: |
          BR="${{ steps.create_branch.outputs.branch }}"

          if ! git apply --index pr.diff; then
            echo "Error: PR diff could not be applied cleanly onto uat." >&2
            exit 1
          fi

          git commit -m "Promote PR #${{ steps.validate.outputs.pr_number }} changes into uat"
          git push origin "HEAD:$BR"

      - name: Build PR body content
        id: body
        run: |
          BR="${{ steps.create_branch.outputs.branch }}"
          PR="${{ steps.validate.outputs.pr_number }}"

          FILES=$(git --no-pager diff --name-only origin/uat..origin/"$BR")
          FILE_COUNT=$(echo "$FILES" | sed '/^\s*$/d' | wc -l | tr -d ' ')

          COMMITS=$(curl -s \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/pulls/${PR}/commits" \
            | jq -r '.[] | "- " + (.commit.message | split("\n")[0]) + " (" + (.sha[0:7]) + ")"')

          echo "Automated Promotion PR" > body.txt
          echo "" >> body.txt
          echo "Source PR: #$PR (merged into dev)" >> body.txt
          echo "Target branch: uat" >> body.txt
          echo "" >> body.txt
          echo "Changes identified: $FILE_COUNT files" >> body.txt
          echo "" >> body.txt
          echo "Changed files:" >> body.txt
          echo "$FILES" >> body.txt
          echo "" >> body.txt
          echo "Commit messages:" >> body.txt
          echo "$COMMITS" >> body.txt
          echo "" >> body.txt
          echo "A PR ID reference will be added below for future uat → prod promotions." >> body.txt

      - name: Create PR into uat
        id: create_pr
        run: |
          BR="${{ steps.create_branch.outputs.branch }}"
          PR="${{ steps.validate.outputs.pr_number }}"
          TITLE="Promote dev → uat (PR #${PR})"

          PAYLOAD=$(jq -n \
            --arg title "$TITLE" \
            --arg head "$BR" \
            --arg base "uat" \
            --arg body "$(cat body.txt)" \
            '{title:$title, head:$head, base:$base, body:$body}')

          RESPONSE=$(curl -s -X POST \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD" \
            "https://api.github.com/repos/${{ github.repository }}/pulls")

          echo "$RESPONSE" > created.json

          PR_NUM=$(jq -r '.number' created.json)
          if [ "$PR_NUM" = "null" ]; then
            echo "Error creating PR: $(jq -r '.message' created.json)" >&2
            exit 1
          fi

          echo "created_pr=$PR_NUM" >> $GITHUB_OUTPUT

      - name: Update PR body with PR ID
        run: |
          PR="${{ steps.create_pr.outputs.created_pr }}"
          UPDATED=updated_body.txt

          cat body.txt > "$UPDATED"
          echo "" >> "$UPDATED"
          echo "PR ID: $PR" >> "$UPDATED"
          echo "Retain this ID for future promotions such as uat → prod." >> "$UPDATED"

          PAYLOAD=$(jq -n --arg body "$(cat $UPDATED)" '{body:$body}')

          curl -s -X PATCH \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD" \
            "https://api.github.com/repos/${{ github.repository }}/pulls/${PR}" > /dev/null

      - name: Completed
        run: |
          echo "Promotion Dev → UAT completed successfully."
